export type RequestsHistoryItem = {
    dateTime: string;
    url: string;
    status: string;
    response: string;
}

export type ExpressionsHistoryItem = {
    dateTime: string;
    expression: string;
}
